echo "#!/bin/bash" >> frostproxy/DEBIAN/postinst
echo "mkdir -p /usr/local/bin" >> frostproxy/DEBIAN/postinst
echo "cp frostproxy /usr/local/bin" >> frostproxy/DEBIAN/postinst
echo "chmod +x /usr/local/bin/frostproxy" >> frostproxy/DEBIAN/postinst